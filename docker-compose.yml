version: '3'

services:
  pfred-service:
    image: pfredimg:latest
    restart: always
    ports:
      - "8080:8080"
    container_name:
      pfredservice
    volumes:
      - pfred-vol:/home/pfred

volumes:
  pfred-vol:
